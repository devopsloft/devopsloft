language: "python"
python:
    - "2.7"
    - "3.5"
install:
    - pip install flake8
    - pip install Flask-WTF
    - pip install coveralls
script:
    - flake8 .

language: shell
script:
  - bash -c 'shopt -s globstar; shellcheck **/*.{sh,ksh,bash}'